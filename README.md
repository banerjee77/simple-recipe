This is very basic recipe web page. The only technical skill used here is very basic HTML syntax.
This will also test the connectivity between local git and centralised github.
The web page will include a heading, a description, ingredients and steps to make the recipe!